version: 2

models:
  - name: bronze_transaction
    config: 
      materialized: view
      schema: bronze
    columns:
      - name: transation_id
        data_tests:
          - unique
          - not_null
          
          